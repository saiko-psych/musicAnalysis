% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_practice_curves.R
\name{plot_practice_curves}
\alias{plot_practice_curves}
\title{Plot interactive practice growth curves}
\usage{
plot_practice_curves(
  long_data,
  wide_data = NULL,
  plot_type = c("individual", "category_sum", "total"),
  categories = NULL,
  category_ids = NULL,
  codes = NULL,
  n_participants = NULL,
  subset_by = c("all", "highest", "lowest", "random"),
  smooth = FALSE,
  facet_by = c("none", "category", "category_id", "participant"),
  category_sum_combined = FALSE,
  color_by = c("code", "category", "category_id"),
  line_color = NULL,
  title = NULL,
  show_legend = TRUE
)
}
\arguments{
\item{long_data}{Long-format data from \code{musical_experience_time()$long},
with columns: \code{code}, \code{category}, \code{category_id}, \code{age_years}, \code{yearly_hours}.}

\item{wide_data}{Optional wide-format data from \code{musical_experience_time()$wide}.
If provided, instrument/singing/othermusic names will be added to legends and hover text.}

\item{plot_type}{Character: what to plot. Options:
\itemize{
\item "individual" (default): separate line for each instrument/singing/other
\item "category_sum": sum by category (instruments/singing/other music)
\item "total": total musical experience (all categories combined)
}}

\item{categories}{Character vector of categories to include (default: all).
Options: "instrument", "singing", "othermusic".
Only used when plot_type = "individual" or "category_sum".}

\item{category_ids}{Numeric vector of category IDs to include (default: all).
Example: \code{c(1, 2)} to show only instrument1, instrument2, singing1, singing2, etc.
Only used when plot_type = "individual".}

\item{codes}{Character vector of participant codes to include (default: all).}

\item{n_participants}{Integer: maximum number of participants to display.
If NULL, shows all selected participants.}

\item{subset_by}{Character: how to select subset when \code{n_participants} is specified.
Options:
\itemize{
\item "all" (default): random subset
\item "highest": participants with highest total practice hours
\item "lowest": participants with lowest total practice hours
\item "random": random subset
}}

\item{smooth}{Logical: if TRUE, applies smoothing to curves (default: FALSE).}

\item{facet_by}{Character: how to organize plots. Options:
\itemize{
\item "none" (default): all curves on one plot
\item "category": separate plot for each category (instrument/singing/othermusic)
\item "category_id": separate plot for each specific instrument/singing/othermusic instance
\item "participant": separate plot for each participant
}}

\item{category_sum_combined}{Logical: when plot_type = "category_sum", should categories
be plotted together in one plot (TRUE) or in separate plots (FALSE, default)?}

\item{color_by}{Character: what to color curves by. Options:
\itemize{
\item "code" (default): each participant gets unique color
\item "category": color by instrument/singing/othermusic
\item "category_id": color by specific instrument number
}}

\item{line_color}{Character: specific color for all lines (optional). If provided,
overrides color_by. Examples: "blue", "#FF5733", "rgb(255,0,0)".}

\item{title}{Character: plot title (default: auto-generated based on plot_type).}

\item{show_legend}{Logical: show legend (default: TRUE).}
}
\value{
A plotly object (interactive plot).
}
\description{
Creates an interactive plotly visualization showing practice hours over age
for selected participants and categories (instruments/singing/othermusic).

Supports three aggregation modes:
\itemize{
\item \code{individual}: separate lines for each participant+instrument/singing/other combination
\item \code{category_sum}: sum hours by category (all instruments, all singing, all other) per participant per age
\item \code{total}: grand total across all musical activities per participant per age
}
}
\examples{
\dontrun{
res <- musical_experience_time("data/survey.csv")

# Total musical experience for all participants
plot_practice_curves(res$long, plot_type = "total")

# Sum by category for top 10 participants
plot_practice_curves(res$long,
                    plot_type = "category_sum",
                    n_participants = 10,
                    subset_by = "highest")

# Individual instruments, faceted by participant
plot_practice_curves(res$long,
                    plot_type = "individual",
                    categories = "instrument",
                    facet_by = "participant",
                    n_participants = 5)

}
}
