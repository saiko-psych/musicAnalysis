% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/musical_experience.R
\name{musical_experience}
\alias{musical_experience}
\title{Combine LimeSurvey musical experience: time + profile}
\usage{
musical_experience(
  file,
  join = c("left", "inner", "full"),
  suffix = c(".time", ".profile"),
  time_args = list(),
  profile_args = list(),
  profile_select_regex = NULL,
  validate = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{file}{Path to the LimeSurvey CSV export.}

\item{join}{One of \code{"left"}, \code{"inner"}, \code{"full"}; default \code{"left"}.}

\item{suffix}{Character vector of length 2 with name suffixes applied on collisions
(\code{c(".time", ".profile")} by default).}

\item{time_args}{Named list of extra arguments forwarded to
\code{\link[=musical_experience_time]{musical_experience_time()}}. Example: \code{list(min_lastpage = 4, weekly_max_hours = 84)}.}

\item{profile_args}{Named list of extra arguments forwarded to
\code{\link[=musical_experience_profile]{musical_experience_profile()}}. Example: \code{list(min_lastpage = 4)}.}

\item{profile_select_regex}{Optional regex; if provided, only profile columns whose
names match this pattern (plus \code{code}) are kept before the merge.
Useful to keep the merged table lean, e.g. \code{profile_select_regex = "^(main_instrument|music_status)"}.}

\item{validate}{If \code{TRUE}, computes a small NA-inflation report for columns that
existed already in the time table.}

\item{verbose}{If \code{TRUE}, prints concise progress messages.}
}
\value{
A list with:
\itemize{
\item \code{sections}: list with the raw section results (\code{time}, \code{profile}).
\item \code{wide}: merged tibble by \code{code}.
\item \code{flags}: forwarded flags from \code{musical_experience_time()} (if any).
\item \code{merge_notes}: tibble with NA-inflation report (or \code{NULL}).
}
}
\description{
Runs the time parser \code{\link[=musical_experience_time]{musical_experience_time()}} and the profile parser
\code{\link[=musical_experience_profile]{musical_experience_profile()}} on the same CSV and safely merges them by \code{code}.
Allows forwarding arguments to each sub-parser and basic post-merge validation.
}
